<Window x:Class="ThanksCardClient.Views.UpdatedEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
        xmlns:v="clr-namespace:ThanksCardClient.Views"
        xmlns:vm="clr-namespace:ThanksCardClient.ViewModels"
        Title="UpdatedEmployee" Height="450" Width="669">

    <Window.DataContext>
        <vm:UpdatedEmployeeViewModel/>
    </Window.DataContext>

    <i:Interaction.Triggers>

        <!--Viewに特別な要件が存在しない限りは、トリガーやアクションの自作にこだわらず積極的にコードビハインドを使いましょう -->
        <!--Viewのコードビハインドは、基本的にView内で完結するロジックとViewModelからのイベントの受信(専用リスナを使用する)に限るとトラブルが少なくなります -->
        <!--Livet1.1からはコードビハインドでViewModelのイベントを受信するためのWeakEventLisnterサポートが追加されています -->

        <!--WindowのContentRenderedイベントのタイミングでViewModelのInitializeメソッドが呼ばれます-->
        <i:EventTrigger EventName="ContentRendered">
            <l:LivetCallMethodAction MethodTarget="{Binding}" MethodName="Initialize"/>
        </i:EventTrigger>

        <!--Windowが閉じたタイミングでViewModelのDisposeメソッドが呼ばれます-->
        <i:EventTrigger EventName="Closed">
            <l:DataContextDisposeAction/>
        </i:EventTrigger>

        <!--WindowのCloseキャンセル処理に対応する場合は、WindowCloseCancelBehaviorの使用を検討してください-->

        <!-- EditUserViewModel からの TransitionMessage("Updated") を受け取とってログオン画面を閉じる処理。 -->
        <l:InteractionMessageTrigger MessageKey="Updated" Messenger="{Binding Messenger}">
            <l:WindowInteractionMessageAction/>
        </l:InteractionMessageTrigger>

        <!-- EditUserViewModel からの TransitionMessage("Deleted") を受け取とってログオン画面を閉じる処理。 -->
        <l:InteractionMessageTrigger MessageKey="Deleted" Messenger="{Binding Messenger}">
            <l:WindowInteractionMessageAction/>
        </l:InteractionMessageTrigger>


    </i:Interaction.Triggers>

    <Grid>
        <StackPanel>

            <StackPanel>
                <TextBlock Text="ユーザー編集" Background="#9ACD32" Foreground="White" FontSize="20" Height="34"/>
            </StackPanel>

            <StackPanel  Orientation="Horizontal">
                <TextBlock Text="課検索" Margin="20,10,0,10" FontWeight="Bold" FontSize="15"/>
                <ComboBox x:Name="SearchSection" ItemsSource="{Binding Sections}" DisplayMemberPath="Name"  SelectedValuePath="Id"  Margin="20,10,0,0" Width="106"  VerticalAlignment="Top" Height="24">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding SectionSearchCommand}" CommandParameter="{Binding ElementName=SearchSection, Path=SelectedValue}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ComboBox>

                <TextBlock Text="名前検索" Margin="20,10,0,10" FontWeight="Bold" FontSize="15"/>

                <ComboBox x:Name="EditEmployee" ItemsSource="{Binding EmployeesInDep}" DisplayMemberPath="Name"  SelectedValuePath="Id" Margin="20,10,0,0" Width="106"  VerticalAlignment="Top" Height="24">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="SelectionChanged">
                            <i:InvokeCommandAction Command="{Binding EditEmployeeCommand}" CommandParameter="{Binding ElementName=EditEmployee, Path=SelectedValue}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </ComboBox>
            </StackPanel>


            <StackPanel Orientation="Horizontal">
                <TextBlock Text="名前" Margin="20,20,0,20" FontWeight="Bold"/>
                <TextBox Text="{Binding Employee.Name}" Margin="20,20,0,20" Width="200" AcceptsReturn="True" Height="28"/>
                <TextBlock Text="メールアドレス" Margin="20,20,0,20" FontWeight="Bold"/>
                <TextBox Text="{Binding Employee.MailAddress}" Margin="20,20,0,24" Width="244" AcceptsReturn="True" Height="28"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="CD    " Margin="20,20,0,20" FontWeight="Bold"/>
                <TextBox Text="{Binding Employee.CD}" Margin="20,20,0,20" Width="200" AcceptsReturn="True" Height="27"/>
                <TextBlock Text="パスワード" Margin="20,20,0,20" FontWeight="Bold"/>
                <TextBox Text="{Binding Employee.Password}" Margin="20,20,0,20"  Width="200" AcceptsReturn="True" Height="28"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Height="48">
                <TextBlock Text="所属課" Margin="20,36,0,-19" FontWeight="Bold" FontSize="15"/>
                <ComboBox ItemsSource="{Binding Sections}" DisplayMemberPath="Name"  SelectedValuePath="Id"  Margin="20,36,0,-19" Width="106"  VerticalAlignment="Top" Height="31" SelectedValue="{Binding Employee.SectionId}"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,10,0" Height="195">
                <Button Content="更新" Command="{Binding Submit2Command}" Background="#6495ED" Foreground="White" Margin="380,100,0,61" Width="59"/>
                <Button Content="削除" Command="{Binding EmployeeDeleteCommand}" Background="#6495ED" Foreground="White" Margin="20,100,0,61" Width="59" CommandParameter="{Binding Employee}"/>
                <Button Content="キャンセル" Command ="{Binding CancelCommand}" Background="#6495ED" Foreground="White"  Margin="20,100,0,61" Width="68" />
            </StackPanel>

        </StackPanel>





    </Grid>
</Window>
